
<h1> {{#if loggedIn}}
    <p> {{username}}'s Home Page</p>
    {{else}}
       <p>Home Page</p>
    {{/if}} </h1>
{{#each blogs}}
    <h3>{{title}}</h3>
    <p>{{body}}</p>
    <p>{{username}}</p>
    <div class="form-floating">
  <textarea class="form-control floatingTextarea" placeholder="Leave a comment here" id={{id}}></textarea>
  <button type="button" class="btn btn-dark" >Comment</button>
</div>
    
    <hr>
{{/each}}